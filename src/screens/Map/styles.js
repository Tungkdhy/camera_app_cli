import { StyleSheet } from "react-native"
export const style = StyleSheet.create({
  container: {
    position: 'relative'
  },
  box_item: {
    position: 'absolute',
    bottom: 0,
    left: 0,
    height: 500,
  }
})